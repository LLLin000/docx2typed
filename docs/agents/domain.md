# Domain Docs

This is a single-context repo.

Before exploring implementation, read:
- `CONTEXT.md`
- relevant files under `docs/adr/`

Use glossary terms from `CONTEXT.md`; surface ADR conflicts instead of silently overriding them.
