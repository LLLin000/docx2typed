# 0029 — Make normalization decisions occurrence-level

The final normalization policy addresses each candidate occurrence, not merely a code point or paragraph. Batch rules may expand to occurrence decisions, but the audit records each old character, replacement, style, context ID, and decision so identical symbols can be handled differently by meaning.
