# 0024 — Validate typed mode against real DOCX fixtures

The primary verification corpus uses Word-generated DOCX samples covering text, formatting, whitespace, anchors, relationships, opaque nodes, tables, sections, and failure paths. Synthetic unit fixtures may supplement it, but rendering screenshots are secondary evidence; package/XML invariants and independent typed verification are the acceptance gate.
