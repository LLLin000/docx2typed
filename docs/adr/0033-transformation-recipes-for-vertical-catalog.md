# 0033 — Model vertical mappings as transformation recipes

Catalog entries describe source text, target text, vertical style delta, confidence class, and reversibility. They may produce multiple target characters and can be manual/ambiguous; the normalizer never assumes a one-codepoint-to-one-character mapping or silently applies compatibility normalization.
